<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>صفحه پیدا نشد | 404</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body { background: #f8fafc; font-family: peyda, sans-serif; }
        .error-container { min-height: 100vh; display: flex; flex-direction: column; justify-content: center; align-items: center; }
        .error-code { font-size: 7rem; font-weight: bold; color: #2d3a4b; }
        .error-message { font-size: 2rem; color: #6c757d; margin-bottom: 2rem; }
        .btn-back { font-size: 1.2rem; padding: 0.75rem 2rem; }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-code">404</div>
        <div class="error-message">صفحه مورد نظر پیدا نشد!</div>
        <a href="/" class="btn btn-primary btn-back"><i class="bi bi-arrow-right"></i> بازگشت به صفحه اصلی</a>
    </div>
</body>
</html> 